{
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "ignoreCommand": "git branch --show-current | grep -q '^news/' && exit 1 || exit 0"
}
